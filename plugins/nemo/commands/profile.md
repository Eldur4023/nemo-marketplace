---
description: Show or update the user's Nemo profile
---

Call the Nemo `boot` tool to load the user's profile and core memory, then show me
the current profile (role, areas, projects, stack, languages, timezone, focus,
preferences).

If I gave an instruction in "$ARGUMENTS", apply it by calling `update_profile` with
the changed fields.
