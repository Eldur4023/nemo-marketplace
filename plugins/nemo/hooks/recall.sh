#!/usr/bin/env bash
# UserPromptSubmit: a light reminder so memory is used by default, not only when
# explicitly asked. The model decides whether to actually call recall.
echo "[Nemo] If answering this needs context about the user, their projects, decisions, conventions, or past work, call the Nemo \`recall\` tool before responding."
